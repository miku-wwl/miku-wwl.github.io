---
interface Props {
	variant?: 'hub' | 'career' | 'projects' | 'vault' | 'lab';
	class?: string;
}

const { variant = 'hub', class: className } = Astro.props;
---

<div class:list={['scene-frame', `scene-${variant}`, className]} data-scene-frame></div>
